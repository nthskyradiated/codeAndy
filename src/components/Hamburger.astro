---
---

<!-- Puts an overlay for hamburger menu -->
<div id="overlay" transition:persist>

</div>

<button id="menu-btn" class="hamburger" type="button" transition:persist>
    <span class="hamburger-top hbg"></span>
    <span class="hamburger-middle hbg"></span>
    <span class="hamburger-bottom hbg"></span>
  </button>

        
    <nav id="mobile-menu" class="mobile-main-menu" aria-label="mobile" transition:persist>
        <ul>
            <li><a href="/" class="" data-astro-reload>Home</a></li>
            <li><a href="/about" class="" data-astro-reload>About</a></li>
            <li><a href="/posts/1" class="" data-astro-reload>Posts</a></li>
        </ul>
        <small><a href="/tag" class="unstyled" data-astro-reload>browse by tag</a></small>
    </nav>

<style lang='scss'>
    @use '../styles/variables' as *;
    @use '../styles/mixins' as *;
.hamburger {
    position: fixed;
    top: 40px;
    right: 20px;
    z-index: 10;
    cursor: pointer;
    width: 20px;
    height: 20px;
    background: none;
    border: none;
    &:hover {
        opacity: .65;
        transition: .4s ease-in-out;
    }
}

.hamburger-top,
.hamburger-middle,
.hamburger-bottom {
    position: absolute;
    width: 20px;
    height: 2px;
    top: 0;
    left: 0;
    background: $secondary-light;
    transition: all 0.5s;
}

.hamburger-middle {
    transform: translateY(5px);
}
.hamburger-bottom {
    transform: translateY(10px);
}

/* Transition hamburger to X when open */

.open {
    transform: rotate(90deg);
}

.open .hamburger-top {
    transform: rotate(45deg) translateY(6px) translateX(6px);
}

.open .hamburger-middle {
    display: none;
}

.open .hamburger-bottom {
    transform: rotate(-45deg) translateY(6px) translateX(-6px);
}

.hbg {
    background: dimgray;
}
/* overlay */

.overlay-show {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5);
    z-index: 3;
}

.mobile-main-menu {
    position: fixed;
    top: 0;
    right: 0;
    width: 350px;
    height: 100%;
    background-color: #000;
    opacity: 0.75;
    z-index: 4;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transform: translateY(-100%);
    transition: transform 0.6s cubic-bezier(0.19, 1, 0.22, 1);
}

li {
    list-style: none;
}

a:not(.unstyled), a:visited{
        text-decoration: none;
        text-transform: uppercase;
        font-size: x-large;
        color: $secondary-light;
    }
@include hover-light($secondary-light, $theme-blue-3);
small {
    padding-top: 24px;
    color: $secondary-light;
    text-decoration: none;
}
.show-menu {
    transform: translateX(0);
}
@media (min-width: 768px){
    .hamburger {
        display: none;
    }
}

</style>

<!-- <script>
import { navigate } from 'astro:transitions/client';

// Navigate to the selected option automatically.
document.querySelector(a).onchange = (event) => {
  let href = event.target.value;
  navigate(href);
};
</script> -->